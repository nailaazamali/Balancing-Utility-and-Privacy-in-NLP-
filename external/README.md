# External baseline code

CusText is maintained by its original authors:

https://github.com/sai4july/CusText

Run `scripts/fetch_custext.sh` to clone it into `external/CusText`. Third-party source code is not copied into this repository.

Candidate outputs can then be converted to the common CSV format and evaluated with the same downstream metrics used for the proposed method.
